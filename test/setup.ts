import "@testing-library/jest-dom/vitest";

// TODO: this is recommended, but doesn't seem to be required yet
// import matchers from "@testing-library/jest-dom/matchers";
// expect.extend(matchers);
